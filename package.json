{
  "name": "rl-toolkit",
  "version": "0.0.0",
  "private": true,
  "description": "Dev tooling for the RL Toolkit. The toolkit itself is a Go binary; this file pins biome (lint + format for the web SDK and plugin sources) so contributors get identical output.",
  "scripts": {
    "format": "biome format --write .",
    "format:check": "biome format .",
    "lint": "biome lint .",
    "lint:fix": "biome lint --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.16",
    "esbuild": "^0.28.0"
  }
}
