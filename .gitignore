# Build artifacts
/rl-toolkit
/rl-toolkit.exe
/backend/rl-toolkit
/backend/rl-toolkit.exe
/release/
/dist/
*.exe
*.rltp

# SDK bundle (built by `make sdk` from sdk/src/)
/backend/internal/server/web/sdk/dist/

# Per-plugin persistence — runtime data, not source
/data/*.json

# Editor / OS noise
.DS_Store
*.swp
*.swo
.idea/
.vscode/

# Tauri / Node (the overlay app lives in overlay/)
overlay/target/
overlay/node_modules/
overlay/src-tauri/target/
overlay/src-tauri/gen/
overlay/src-tauri/data/
# Fixed-version WebView2 runtime fetched at build time by the
# launcher-installer make target; bundles into the NSIS installer to
# dodge the WebView2 144+ ghost-titlebar regression on transparent
# windows (tauri-apps/tauri#14764). Never committed.
overlay/src-tauri/webview2-runtime/
overlay/src-tauri/webview2-runtime.nupkg
**/node_modules/

# Local agent / session notes / archives
.claude/
*.zip
docs/superpowers/

# Local git worktrees
.worktrees/

# Local helper scripts (not for distribution)
/scripts/
